## Agent skills

### Issue tracker

Issues and PRDs are stored as local Markdown under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

The repository uses the five default triage roles. See `docs/agents/triage-labels.md`.

### Domain docs

This is a multi-context repository with four independently runnable modules. See `docs/agents/domain.md`.
